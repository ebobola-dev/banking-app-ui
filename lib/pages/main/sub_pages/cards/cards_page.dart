import 'package:flutter/material.dart';

class CardsSubPage extends StatelessWidget {
  const CardsSubPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
